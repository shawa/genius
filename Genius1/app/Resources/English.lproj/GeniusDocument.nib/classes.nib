{
    IBClasses = (
        {
            ACTIONS = {add = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = GeniusArrayController; 
            LANGUAGE = ObjC; 
            OUTLETS = {geniusDocument = id; }; 
            SUPERCLASS = NSArrayController; 
        }, 
        {
            ACTIONS = {
                add = id; 
                duplicate = id; 
                endDeckPreferences = id; 
                exportFile = id; 
                quizAutoPick = id; 
                quizReview = id; 
                quizSelection = id; 
                resetScore = id; 
                search = id; 
                toggleStatusText = id; 
            }; 
            CLASS = GeniusDocument; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                arrayController = GeniusArrayController; 
                deckPreferences = NSWindow; 
                infoDrawer = NSDrawer; 
                initialWatermarkView = NSView; 
                learnReviewSlider = NSSlider; 
                levelField = NSTextField; 
                levelIndicator = NSLevelIndicator; 
                notesDrawer = NSDrawer; 
                statusField = NSTextField; 
                tableView = MyTableView; 
            }; 
            SUPERCLASS = NSDocument; 
        }, 
        {CLASS = GeniusItem; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = LevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            CLASS = MyTableView; 
            LANGUAGE = ObjC; 
            OUTLETS = {documentController = id; }; 
            SUPERCLASS = NSTableView; 
        }, 
        {CLASS = NSLevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSControl; }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}