{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = GeniusRelevanceIndicator; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {getRightNo = id; getRightSkip = id; getRightYes = id; handleEntry = id; }; 
            CLASS = MyQuizController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                answerTextView = NSTextView; 
                associationController = NSObjectController; 
                cueTextView = NSTextView; 
                entryField = NSTextField; 
                evaluationTabView = NSTabView; 
                noButton = NSButton; 
                progressIndicator = SimpleProgressIndicator; 
                yesButton = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = SimpleProgressIndicator; LANGUAGE = ObjC; SUPERCLASS = NSView; }
    ); 
    IBVersion = 1; 
}